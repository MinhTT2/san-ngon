import { BrandLoader } from '@/components/brand-loader';

/** Next hiện file này trong lúc chờ server component của route render xong. */
export default function Loading() {
  return <BrandLoader />;
}
